//the interfaces provides the parameters that the http methods call

export interface Employee {
  id: number;
  name: string;
  email: string;
  jobTitle: string;
  phoneNumber: string;
  imageUrl: string;
  employeeCode: string;
}
